# Media Path Governance

Phase 14C defines thumbnail and normalized image path abstractions.

The plan uses a managed cache root design and preserves legacy path fallback. It does not regenerate thumbnails, regenerate normalized images, or move old files.

Phase 15A should complete Windows and macOS packaging release flow planning.
