# External Inference First Runtime Governance

AI runtime governance prefers user-configured external HTTP runtimes before managed local Llama installation or automatic Python Worker launch. This keeps network checks user-triggered and local downloads, installs, and service starts explicit, trading convenience for predictable privacy, platform safety, and cross-platform support.
